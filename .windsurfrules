# Fix Lang Project Rules

## System

- macOS (Apple Silicon)
- Electron

## Technical Stack

- Electron
- TypeScript
- React
- OpenAI API
- Tailwind CSS
- Vite
- PNPM (package manager)

## General Rules

- Always start responses with `I’ve already read the rules\n`
- Always read `plan.md` before writing any code
- After adding a major feature or completing a milestone, update `plan.md`
- Search Memories first before exploring the codebase
- Actively store best practices, patterns, and solutions in Memories for future use
- If you repeatedly search for certain files or patterns, add them to Memories for future reference
- Analyze each task before starting:
  - Plan the implementation
  - Break the task into smaller steps
  - Add the task to `plan.md` or related files as a TODO list
- Use `plan.md` as a guide, but don’t follow it too rigidly
- Review `src/main/index.ts`, `src/preload/index.ts`, and `electron.vite.config.ts` to understand the codebase structure before starting
- Check `package.json` to review dependencies before requesting package installations
- Mark the task in `plan.md` or related files complete upon finishing, including a summary of the implementation and any accessibility notes

## Coding Rules

- Use TypeScript for all code
- Use Tailwind CSS for styling
- Prefer `type` over `interface`
- Follow SOLID principles; keep code simple, maintainable, and testable
- Use descriptive variable names
- Prefer `const` over `function` for declarations
- Keep files under 200 lines, split into smaller files if necessary
